config:
  target: "http://localhost:3010/sidebar/postalCode"
  phases:
    - duration: 20
      arrivalRate: 3
  payload:
    path: "postal_codes_50000.csv"
    fields:
      - "id"
scenarios:
  - name: "Business"
    flow:
      - get:
          url: "/{{ id }}"